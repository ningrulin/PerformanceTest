<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">


<scenario name="Basic Sipstone UAC">
  <!-- In client mode (sipp placing calls), the Call-ID MUST be         -->
  <!-- generated by sipp. To do so, use [call_id] keyword.                -->
  <!--m=audio [media_port] RTP/AVP 8 101  pcma -->
  
  <send retrans="500">
    <![CDATA[

        INVITE sip:[field1]%3a[remote_ip]@[remote_ip]:[remote_port] SIP/2.0
        Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
        From: [field3] <sip:[field0]@[remote_ip]:[remote_port]>;tag=[call_number]
        To: sip:[field1]@[remote_ip]:[remote_port]
        CSeq: 20 INVITE
        Call-ID: [call_id]
        Max-Forwards: 70
        Supported: outbound
        Allow: INVITE, ACK, CANCEL, OPTIONS, BYE, REFER, NOTIFY, MESSAGE, SUBSCRIBE, INFO, UPDATE
        Content-Type: application/sdp
        Content-Length: [len]
        Contact: <sip:[field0]@[local_ip]:[local_port]>
        User-Agent: Linphone/3.9.1 (belle-sip/1.4.2)

        v=0
        o=user1 3502 2915 IN IP[local_ip_type] [local_ip]
        s=Talk
        c=IN IP[media_ip_type] [media_ip]
        t=0 0
        a=rtcp-xr:rcvr-rtt=all:10000 stat-summary=loss,dup,jitt,TTL voip-metrics
        m=audio [auto_media_port] RTP/AVP 8 101
        a=rtpmap:101 telephone-event/800
        m=video [auto_media_port+2] RTP/AVP 96
        a=rtpmap:96 H264/90000
        a=fmtp:96 profile-level-id=42801F

    ]]>
  </send>

  <recv response="100" optional="true">
  </recv>

  <recv response="180" optional="true">
  </recv>

  <recv response="183" optional="true">
  
  </recv>

  <!-- By adding rrs="true" (Record Route Sets), the route sets         -->
  <!-- are saved and used for following messages sent. Useful to test   -->
  <!-- against stateful SIP proxies/B2BUAs.                             -->
  <recv response="200" rtd="true">
     <action>
         <ereg regexp="From: 10010000703*" search_in="msg" check_it="true" assign_to="10"  value="7"/>
      </action>
  </recv>

  <!-- Packet lost can be simulated in any send/recv message by         -->
  <!-- by adding the 'lost = "10"'. Value can be [1-100] percent.       -->
  <send>
    <![CDATA[

        ACK sip:[field1]@[remote_ip]:[remote_port];transport=[transport] SIP/2.0
        Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
        From: <sip:[field0]@[remote_ip]>;tag=[call_number]
        To: <sip:[field1]@[remote_ip]>[peer_tag_param]
        CSeq: 20 ACK
        Call-ID: [call_id]
        Max-Forwards: 70

    ]]>
</send>
<nop hide="true">
<action>
	<exec command = "echo [$10]" />
  </action>
</nop>

  <nop hide="true">
		<action>
		  
                  <exec play_pcap_audio = "/mnt/aqf/aqf/aaa/PerformanceTest/SipOperation/sipxml/pcap/a.pcap" />
		  <add assign_to="1" value="1" />
		  <test assign_to="result" variable="1" compare="less_than" value="5" />
		</action>
  </nop>

  <pause milliseconds="1000"/>
  <nop next="jump" test="result" />

  <!-- This delay can be customized by the -d command-line option       -->
  <!-- or by adding a 'milliseconds = "value"' option here.             -->

  <label id="jump"/>
  <nop>
	<action>
		<jump value="6"/>
	</action>
  </nop>

  <!-- The 'crlf' option inserts a blank line in the statistics report. -->
  <send retrans="500">
    <![CDATA[

        BYE sip:[field1]@[remote_ip]:[remote_port];transport=[transport] SIP/2.0
        Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch];rport
        From: <sip:[field0]@[local_ip]>;tag=[call_number]
        To: <sip:[field1]@[remote_ip]>[peer_tag_param]
        CSeq: 21 BYE
        Call-ID: [call_id]
        Max-Forwards: 70
        User-Agent: Linphone/3.9.1 (belle-sip/1.4.2)
      
    ]]>
  </send>

  <recv response="200" crlf="true">
  </recv>



  <!-- definition of the response time repartition table (unit is ms)   -->
  <ResponseTimeRepartition value="10, 20, 30, 40, 50, 100, 150, 200"/>

  <!-- definition of the call length repartition table (unit is ms)     -->
  <CallLengthRepartition value="10, 50, 100, 500, 1000, 5000, 10000"/>

</scenario>

